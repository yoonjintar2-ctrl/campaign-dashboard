/* Supabase 접속 정보.
   이 파일을 config.js 로 복사한 뒤 두 값을 채우세요.
   anon key 는 브라우저에 노출되는 것이 정상입니다 — 실제 데이터 보호는 RLS(schema.sql)가 합니다.
   절대 넣으면 안 되는 값: service_role key. */
window.CLOUD_CONFIG = {
  url:     "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-KEY"
};
